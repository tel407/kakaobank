
dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation project(':module-api')
}
bootJar{
    archivesBaseName = 'BlogServiceApp'
    archiveFileName = 'BlogServiceApp.jar'
    archiveVersion = "0.0.1"
}